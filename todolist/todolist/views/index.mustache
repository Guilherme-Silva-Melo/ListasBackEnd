<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>TODO APP</title>
  </head>
  <body>
    <h1>TODO APP</h1>
    <h2>Tarefas</h2>
    <ul>
      {{#tasks}}
        <li>{{name}} 
          <a class="
            {{#isBaixa}}text-success{{/isBaixa}} 
            {{#isMedia}}text-warning{{/isMedia}} 
            {{#isAlta}}text-danger{{/isAlta}} 
          ">{{priority}}</a>
          <a href="/?tid={{id}}">edit</a> 
          <a href="/tarefas/del/{{id}}">done</a>
      {{/tasks}}
    </ul>
    
    <h2>Nova tarefa</h2>
    <form action="/tarefas" method="POST">
        <label for="prioridade">Prioridade:</label>
        <select name="prioridade" id="prioridade" style="background-color">
          <option value="Baixa" style="background-color: green">Baixa</option>
          <option value="Media" style="background-color: yellow">Media</option>
          <option value="Alta" style="background-color: red">Alta</option>
        </select>
        {{#task.id}}
        <input type="hidden" name="id" value="{{.}}">
        {{/task.id}}
        <label for="nome">Nome:</label>
        <input type="text" id="nome" name="nome" value="{{task.name}}"/>
        <input type="submit" value="Salvar" class="btn-primary"/>
    </form>
    <p>{{erro}}</p>
  </body>
</html>